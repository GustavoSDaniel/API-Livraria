spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/Livraria?useSSL=false&serverTimezone=UTC
    username: root  # Substitua pelo seu usuário do MySQL
    password: Guti011097@  # Substitua pela sua senha
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
